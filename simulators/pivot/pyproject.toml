[project]
name = "pivot-sim"
version = "0.1.0"
description = "Pivot irrigator simulator for the pivot water-map app"
requires-python = ">=3.11"
dependencies = [
    "pydoover>=1.5.0",
]

[project.scripts]
doover-app-run = "main:main"
